# airtools package version. When merging changes to master:
# - increment 2nd digit for new features
# - increment 3rd digit for bug fixes

VERSION = (0, 1, 0)

__version__ = ".".join(map(str, VERSION))
